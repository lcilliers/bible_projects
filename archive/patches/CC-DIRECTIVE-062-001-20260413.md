# CC-DIRECTIVE-062-001 — Registry 62 (fellowship) — Re-export and Investigation
**Date:** 2026-04-13
**Issued by:** Claude AI — Session B Stage 1 Audit
**Governing instruction:** WA-SessionB-Instruction-v4.7-2026-04-12
**Registry:** 62 — fellowship

---

## Background

Stage 1 audit of `wa-062-fellowship-complete-2026-04-13.json` identified three export gaps and one data integrity question requiring investigation before Session B Stage 2 can proceed.

---

## ACTION 1 — Re-export (mandatory)

**Produce a fresh complete export for Registry 62 (fellowship).**

Filename: `wa-062-fellowship-complete-2026-04-13-r2.json`

The current export has three structural gaps that must be resolved in the re-export:

### Gap A — verse_context context_records missing

Every verse context group in the export has an empty `context_records` array. The statistics block correctly states 95 verse_context records (22 anchors, 31 related, 42 set-aside) — these exist in the database but were not included in the export.

**Required:** Populate `context_records` for every group in `verse_context.groups`. Each record must include at minimum: `verse_record_id`, `reference`, `verse_text`, `is_anchor`, `is_relevant`, `is_related`, `set_aside_reason`, `notes`.

### Gap B — correlations block partner fields null

The correlations block is structurally present with correct counts (21 cooccurrence pairs, 29 dimension overlap pairs, 13 shared anchor pairs, 1 xref pair) but all partner data is null throughout: `partner_registry_id`, `partner_word`, `dimension`, `our_group_code`, `their_group_code`, `verse_count` are null across all arrays.

**Required:** Resolve all partner registry identifiers and populate all fields in:
- `correlations.xref_sharing` — partner_registry_id, partner_word, shared_strongs, shared_term_count
- `correlations.verse_cooccurrence` — partner_registry_id, partner_word, verse_count, sample_verses
- `correlations.dimension_overlap` — partner_registry_id, partner_word, dimension, our_group_count, their_group_count
- `correlations.shared_anchor_verses` — partner_registry_id, partner_word, reference, our_group_code, their_group_code

### Gap C — wa_term_inventory fields missing from term records

For all 15 terms, the following `wa_term_inventory` fields are null in the export: `term_owner_type`, `delete_flagged`, `god_as_subject`, `somatic_link`, `gloss` (inventory-level).

**Required:** Include all `wa_term_inventory` fields in each term record in the re-export.

---

## ACTION 2 — Investigate H2269 (cha.var — fellow) delete status

**Finding:** H2269 has `mti_terms.status = delete` but has active verse context records in the database (groups 2689-001 and 2689-002, 4 relevant verses, classified in VCB-037). The xref_sharing signal also shows H2269 as shared with one partner registry.

**Question:** Is this a pending cascade (mti deleted but wa_term_inventory.delete_flagged = 0, cascade not yet applied) or an error in the deletion?

**Required investigation:**
1. Query `wa_term_inventory` for H2269, Registry 62: what is `delete_flagged`?
2. Query `verse_context` for mti_term_id corresponding to H2269: how many records exist, and what is their content?
3. Check `mti_terms` exclusion_reason for H2269: what is the documented rationale for deletion?
4. Check the xref_sharing partner: which registry shares H2269, and what is its term_owner_type there?

**Report back:** Provide findings on all four points. Claude AI will make the analytical determination on whether to confirm or challenge the deletion after reviewing the findings.

---

## ACTION 3 — Field-level update (apply alongside re-export)

Update the following fields on `word_registry` for Registry 62:

| Field | Current value | Required action |
|---|---|---|
| `dimensions` | "Relational/Social" | Update to reflect confirmed Dimension Review results: "Relational Disposition; Moral Character; Transformation; Divine-Human Correspondence; Emotion — Positive; Cognition" |
| `unique_term_count` | 2 | Update to 14 (pending H2269 investigation — if H2269 reinstated: 14 remains correct; if confirmed deleted: update to 13) |
| `shared_term_count` | 0 | Verify after H2269 investigation |
| `term_sharing_ratio` | 0.0 | Recalculate after above |

**Note:** Do not apply the `unique_term_count` update until H2269 investigation (Action 2) is complete and Claude AI has confirmed the deletion decision.

---

## Confirmation required

Please confirm:
1. Re-export complete — filename and location
2. H2269 investigation findings — all four points answered
3. `dimensions` field updated on word_registry

Claude AI will review the re-export and H2269 findings before issuing any further patches or proceeding to Stage 2.
