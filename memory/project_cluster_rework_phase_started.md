---
name: project_cluster_rework_phase_started
description: 2026-06-05 — START of the full cluster-rework phase. Rework EVERY cluster M01-upward, each made the best it can be (redo where needed), with SPECIAL ATTENTION to verse meaning and qualitative findings. New home Sessions-v2/{CODE}-{Name}/; old Sessions/ read-only cross-ref; pre-phase DB backup taken.
metadata:
  type: project
---

**Phase anchor — 2026-06-05.** The programme enters a deliberate **rework of every cluster**, in order
**M01 → M46** (plus FLAG, T2). Each cluster is made **the best it can be** — a prior "Analysis Completed"
status is *not* a reason to skip; clusters are **re-done where needed**. **Special attention to verse
meaning and qualitative findings** (the gravest risk; the keyword/meaning transgressions found 2026-06-04
are why this phase exists).

**Operating frame:**
- **Home:** all new per-cluster output → `Sessions-v2/{CODE}-{Name}/` (48 stub folders created). Old
  `Sessions/` tree (incl. `Session_Clusters/`, `Session_A…D/`) is **intact, read-only cross-reference**.
  Filing rule: `docs/file-organisation-rules.md` §3.0; CLAUDE.md directory map updated.
- **Method:** structural cluster audit (aspect spec Groups A–D) to bank clusters to **clean** status
  first, then the **meaning/keyword-quality audit (Group E)** as a later phase
  ([[project_audit_sequencing_clusters_then_meaning]]). Findings live only in the DB
  ([[reference_analysis_rules_finding_lifecycle]]); interdependent clusters finalised together
  ([[feedback_interdependent_clusters_finalise_together]]).
- **Fallback point:** pre-phase DB backup taken and SHA256-verified →
  `C:\Users\lerouxc\db_recovery\bible_research_pre_clusterrework_20260605.db` (DB integrity ok, schema
  3.28.0). Fall back here to undo the new phase.

Grounded in the settled foundations ([[reference_study_definition_and_nine_principles]],
[[reference_study_end_point_and_milestones]]). Next concrete step: run the structural audit on **M01-Fear**.
