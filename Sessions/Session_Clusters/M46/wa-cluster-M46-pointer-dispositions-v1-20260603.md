# M46 (Abundance) — pointer disposition evaluation — FOR RESEARCHER DECISION (held, not applied)

**Date:** 2026-06-03 · **Method:** evaluated on text. **M46 is NOT a clear-cut case** — surfaced for
your decision rather than auto-applied, because its A7 is dominated by *substantive legacy peace
synthesis findings*, not the superseded `DIMENSION_REVIEW` notes seen in M06/M08.

## What M46 actually contains
Almost everything under M46 is **registry-117 (peace)** content, surfacing here only because a peace
term sits in an M46 sub-group (non-exclusive term→subgroup link). It is **peace (M33)**, not abundance.

### A6 — 21 flags (all reg-117 peace pointers) — CLEAR-CUT
- 19 are `[v1.8 obslog SD pointer]` peace-connection pointers, `cluster_link` mostly **M33** → **rehome → M33** (their content-derived target; a few carry secondary links e.g. 644 M05,M33 / 660 M04,M33 — preserve the multi-link for M33's analysis to resolve as cross-cluster).
- **119, 120** — YHWH-Jireh / YHWH registration anomalies (data notes, not analytical) → **resolve**.

### A7 — 31 findings
- **28 = legacy peace Session-B v1.8 synthesis** (`SYNTHESIS_INTRA_TIER` ×7, `SYNTHESIS_INTER_TIER` ×21; reg 117): the actual tier-by-tier peace analysis (T1–T7). **Substantive — must NOT be set aside as noise.** They belong to **M33 (Peace)**.
- **2 = peace `DIMENSION_REVIEW`** (29, 143; reg 117) — peace structural notes → with the synthesis.
- **1 genuinely M46: finding 39** (contentment, reg 29) — "stable inner condition independent of circumstances" = M46 char-116 "inner life beyond material circumstance" → **adopt as M46 observation + fold**.

## The decision I need (no precedent exists for this)
The 06-01/06-02 dispositions only ever handled `DIMENSION_REVIEW` + `SD_POINTER`. **There is no
established disposition for legacy `SYNTHESIS_*` findings under the cluster model.** Options for the
28 peace-synthesis findings (+ the 2 peace DIM notes):

- **(a) Route to M33** (`status='routed_cluster'`, `cluster_link='M33'`) — non-lossy; preserves them as input for M33's eventual analysis; clears M46's gate. *(my lean — consistent with your anti-lossy / dual-cluster stance)*
- **(b) Set aside** as superseded by the cluster model (M33 will regenerate its own findings) — cleaner but discards the v1.8 peace synthesis.
- **(c) Something else** — e.g. a dedicated "legacy-synthesis" handling, or first investigate whether reg-117 (peace) is even correctly assigned to an M46 sub-group (the root cause of all this surfacing).

## Likely root cause worth noting
That *all* of M46's pointers/findings are reg-117 peace strongly suggests a **peace term is (mis?)assigned to an M46 sub-group**. Worth checking whether that sub-group membership is correct before routing — if the peace term shouldn't be in M46 at all, the cleaner fix is upstream (term→subgroup), not pointer-routing.

**Held. Nothing applied.** Tell me (a)/(b)/(c) and whether to check the reg-117→M46 sub-group assignment first.
